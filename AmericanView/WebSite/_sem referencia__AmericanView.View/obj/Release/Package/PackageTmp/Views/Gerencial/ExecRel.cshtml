@model IEnumerable<Keeptrue.Conciliador.Relatorios.Application.Dto.GerencialCreditoDto>

@{
    ViewBag.Title = "ExecRel";
}

<div id="modalFechamentoLog" class="modal fade">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <center><h4>Relatório Gerencial<strong></strong></h4></center>
            </div>
            <div>
                @foreach (var item in Model)
                {
                    @Html.DisplayNameFor(model => model.Ordem)
                    @Html.DisplayFor(modelItem => item.Ordem)
                    @Html.DisplayFor(modelItem => item.Linha)
                    @Html.DisplayFor(modelItem => item.SPED)
                    @Html.DisplayFor(modelItem => item.Descricao)
                    @Html.DisplayFor(modelItem => item.Base)
                    @Html.DisplayFor(modelItem => item.Pis)
                    @Html.DisplayFor(modelItem => item.Cofins)
                    @Html.DisplayFor(modelItem => item.TribMI)
                    @Html.DisplayFor(modelItem => item.NTribMI)
                    @Html.DisplayFor(modelItem => item.NTribEx)
                    @Html.DisplayFor(modelItem => item.Tipo)
                    @Html.DisplayFor(modelItem => item.Totalizador)
                    @Html.DisplayFor(modelItem => item.Rateio)

                }
            </div>
        </div>
    </div>
</div>

